package io.github.rakellkizz.auth_api; // Declaração do pacote onde a classe está localizada

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Classe principal da aplicação Spring Boot.
 *
 * A anotação {@link SpringBootApplication} marca esta classe como a principal do projeto,
 * ativando automaticamente a configuração, a varredura de componentes e a configuração do Spring Boot.
 */
@SpringBootApplication
public class AuthApiApplication {
	public static void main(String[] args) {
		// Código de inicialização do seu aplicativo
	}

	/**
	 * Método principal da aplicação.
	 * Ele inicia a aplicação Spring Boot, carregando todas as configurações e gerenciando
	 * o ciclo de vida da aplicação.
	 *
	 * @param args Argumentos da linha de comando passados na execução.
	 */
	}

